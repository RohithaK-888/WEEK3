#include <stdio.h>

int main() {
   int a, b, c;
   printf("REG NO:25331A05F7\n");
   printf("enter the numbers:\n");
   scanf("%d %d %d", &a, &b, &c);

   if(a > b && a > c)
       printf("max=%d", a);
   else if(b > c)
       printf("max=%d", b);
   else
       printf("max=%d", c);

   return 0;
}
